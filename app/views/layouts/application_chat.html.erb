<!DOCTYPE html>
<html data-theme>
  <head>
    <title>Newerparp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://cdn.jsdelivr.net/gh/alumuko/vanilla-datetimerange-picker@latest/dist/vanilla-datetimerange-picker.js"></script>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class="min-h-screen max-h-screen flex flex-col overflow-hidden">
      <div class="bg-base-100 bg-global bg-no-repeat bg-fixed bg-cover grow md-4 flex flex-row">
        <div class="flex flex-col space-y-4 grow">
          <div class="opacity-90 text-base-content md:p-4 flex flex-row grow">
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
